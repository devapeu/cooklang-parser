# Cooklang Typescript

Yet another parser in JS/TS for cooklang.

**TODO**
- add support for preparation short-hands (e.g. @onion{1}(peeled and finely chopped))
- use == to mark a section instead of >>
- correctly parse fractions (e.g 1/2 => 0.5)
- process metadata